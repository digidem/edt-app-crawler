# Earth Defenders Toolkit App Crawler

## Usage

Install:
`npm i -g edt-app-crawler`

Usage:
`app-crawler --path="/tmp/apps"`

## Developing

`npm i`

`npm run dev` will run a fake download.

Apps will be available on `src/apps`